<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="redsix.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:redsix.Views"
    xmlns:views="clr-namespace:PDS.App.RedSix.Views"
    Shell.FlyoutBehavior="Disabled">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:Sticky}"
        Route="MainPage" />
        <!--ContentTemplate="{DataTemplate views:DomTestPage}"-->
        <!--ContentTemplate="{DataTemplate local:MoodSet}"-->
        <!--ContentTemplate="{DataTemplate local:SignUp}"-->
        <!--ContentTemplate="{DataTemplate local:Login}-->
</Shell>
